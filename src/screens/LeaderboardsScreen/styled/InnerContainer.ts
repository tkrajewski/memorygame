import styled from 'styled-components';

import InnerContainer from '../../../components/InnerContainer';

export default styled(InnerContainer)`
  align-items: center;
  display: flex !important;
  height: 100%;
  flex-direction: column;
  justify-content: center;
`;
